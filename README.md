# Сервис агрегации данных и мониторинга контроля качества рам (Демонстрационная версия)

Данный микросервис разработан на платформе Spring Boot и является центральным узлом демонстрационной версии (MVP) системы автоматизированного технического контроля (ОТК) завода. Сервис отвечает за прием результатов анализа геометрии и дефектов рам, ведение истории проверок, расчет оперативной статистики конвейера и передачу данных на верхний уровень (интерфейс оператора).

Проект разработан в рамках производственной практики.

## Архитектурная роль в киберфизическом комплексе
1. **Шина данных**: Брокер сообщений Apache Kafka передает асинхронные события с результатами компьютерного зрения (смежный сервис на Python) в топик `frame-check-results`.
2. **Обработка данных (Данный сервис)**: Компонент `FrameResultEventConsumer` асинхронно считывает JSON-события. Сервисный слой трансформирует их в доменные сущности `FrameCheckEntity`, классифицирует статус (`NORMAL` / `DEFECT`) и сохраняет в базу данных через Spring Data JPA.
3. **Верхний уровень (SCADA / MES)**: Сервис предоставляет REST API для инициализации дашборда и открывает постоянный канал Server-Sent Events (SSE) для мгновенного обновления интерфейса оператора ОТК при обнаружении брака.

Интеграция со смежным микросервисом: [Репозиторий сервиса машинного зрения](https://github.com/Diana-debug-ship-it/Defect-Control-System-Demo.CV-service)

---

## Технологический стек
* **Java 25**
* **Spring Boot 4.1.0**
* **Apache Kafka**
* **Hibernate**
* **PostgreSQL**

# Реализованный REST API Эндпоинты

### Начальная загрузка дашборда
* `GET /api/v1/quality-control/stats` — Возвращает сводную статистику конвейера (общее количество рам, годные, брак).
* `GET /api/v1/quality-control/history` — Возвращает хронологический список последних проверок рам для таблицы истории.

---

Для тестирования запустите бэкенд и симулятора конвейера на Python, откройте веб-интерфейс по адресу: `http://localhost:8080/`.
